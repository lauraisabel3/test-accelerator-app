import React from "react";
import { Container, Logo, IconMenu } from './styles'

const Header = () => {
  return(
    <Container>
      <Logo />
      <IconMenu />
    </Container>
  )
}

export default Header